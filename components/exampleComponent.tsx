/** Info file - to be deleted later */

// components folder stores all your reusable UI code
// e.g. you built a killer navbar and you wanna reuse that all over your app on every page. put that component in this folder

import React from "react";

const exampleComponent = () => {
  return <div>exampleComponent</div>;
};

export default exampleComponent;
